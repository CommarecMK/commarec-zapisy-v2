[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 4 --worker-class gthread --threads 4 --timeout 0"
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
